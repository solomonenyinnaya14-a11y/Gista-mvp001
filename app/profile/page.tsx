"use client";
import {useEffect,useState} from "react";
import Link from "next/link";
import {createClient} from "@/lib/supabase/client";
import {signOut} from "@/lib/auth";
export default function ProfilePage(){
 const [profile,setProfile]=useState<any>(null); const [edit,setEdit]=useState(false); const [displayName,setDisplayName]=useState(""); const [bio,setBio]=useState(""); const [stats,setStats]=useState({gists:0,followers:0,following:0}); const [loading,setLoading]=useState(true); const supabase=createClient();
 useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser(); if(!user)return; const {data}=await supabase.from("profiles").select("username,display_name,bio,avatar_url").eq("id",user.id).single(); setProfile(data); setDisplayName(data?.display_name??""); setBio(data?.bio??""); const [g,f1,f2]=await Promise.all([supabase.from("posts").select("id",{count:"exact",head:true}).eq("author_id",user.id),supabase.from("follows").select("follower_id",{count:"exact",head:true}).eq("following_id",user.id),supabase.from("follows").select("following_id",{count:"exact",head:true}).eq("follower_id",user.id)]); setStats({gists:g.count??0,followers:f1.count??0,following:f2.count??0}); setLoading(false)})()},[]);
 return <main className="profile-page"><header className="simple-header"><Link href="/">‹ Home</Link><strong>Profile</strong><button>⚙</button></header><section className="profile-card">{loading?<p>Loading profile…</p>:<><div className="profile-avatar">{profile?.display_name?.[0]?.toUpperCase()??"G"}</div><h1>{profile?.display_name??"Gista User"}</h1><p>@{profile?.username??"username"}</p><p className="bio">{profile?.bio||"A place to talk, share and connect."}</p><div className="profile-stats"><span><b>{stats.gists}</b> Gists</span><span><b>{stats.followers}</b> Followers</span><span><b>{stats.following}</b> Following</span></div><button className="primary small" onClick={()=>setEdit(!edit)}>Edit profile</button>{edit&&<div className="create-card"><input value={displayName} onChange={e=>setDisplayName(e.target.value)} placeholder="Display name"/><textarea value={bio} onChange={e=>setBio(e.target.value)} placeholder="Bio"/><button className="primary small" onClick={async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {error}=await supabase.from("profiles").update({display_name:displayName,bio}).eq("id",user.id);if(!error){setProfile({...profile,display_name:displayName,bio});setEdit(false)}}}>Save profile</button></div><button className="primary small" onClick={signOut}>Log out</button></>}</section></main>}