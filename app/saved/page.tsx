"use client";
import {useEffect,useState} from "react";
import Link from "next/link";
import {ArrowLeft,Bookmark} from "lucide-react";
import {createClient} from "@/lib/supabase/client";
export default function SavedPage(){const supabase=createClient();const [posts,setPosts]=useState<any[]>([]);const [loading,setLoading]=useState(true);
useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user){location.href="/auth";return}const {data}=await supabase.from("saves").select("post_id,posts(id,body,content_type,media_url,category,created_at,profiles(display_name,username))").eq("user_id",user.id).order("created_at",{ascending:false});setPosts((data??[]).map((x:any)=>x.posts).filter(Boolean));setLoading(false)})()},[]);
return <main className="content"><header className="simple-header"><Link href="/"><ArrowLeft size={18}/></Link><strong>Saved</strong><Bookmark size={18}/></header>{loading?<p>Loading saved Gists…</p>:posts.length===0?<div className="empty-state"><Bookmark size={32}/><h2>No saved Gists yet</h2><p>Save a Gist and it will appear here.</p><Link href="/">Discover Gists</Link></div>:<div className="feed">{posts.map((p:any)=><Link className="post" key={p.id} href={"/gist/"+p.id}><div className="post-head"><div className="avatar">{p.profiles?.display_name?.[0]?.toUpperCase()??"G"}</div><div className="identity"><strong>{p.profiles?.display_name??"Gista User"}</strong><span>@{p.profiles?.username??"user"} · {new Date(p.created_at).toLocaleString()}</span></div><span className="category">{p.category}</span></div>{p.content_type==="photo"&&p.media_url&&<img src={p.media_url} alt="Saved Gist" style={{width:"100%",borderRadius:16}}/>}{p.content_type==="voice"&&p.media_url&&<audio controls src={p.media_url} style={{width:"100%",marginTop:10}}/>}{p.body&&<p className="post-text">{p.body}</p>}</Link>)}</div>}</main>}